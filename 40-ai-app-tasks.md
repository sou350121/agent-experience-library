# AI 应用开发监控任务群总结文档

> 本文档为 2026-02-19 rev.12 更新版。
> 2026-02-19 rev.12：全系统 post script account argparse default 加固 + root ownership 修复。
> 2026-02-19 rev.11：全系统 Telegram account 路由修复（7 个任务长期发到错误频道）。
> 当前 AI App 管道共 11 个 job（含共享的 Gateway Preflight 和 Watchdog）。

---

## 1. 系统定位

AI 应用监控任务群负责：
- **采集层**：稳定源 RSS 收集（低成本、可持续）
- **日报层**：每日监控日报 + 社交情报 + 每日精选
- **深度层**：Deep Dive 深度分析文章（每周 3 篇）+ 周报
- **推理层**：双周报告 + 反思问题（跨期推理与知识巩固）

与 VLA 管道对称设计，共享 Gateway Preflight、Daily Watchdog 等运维任务。

---

## 2. 当前任务清单（Asia/Shanghai）

### 2.1 每日任务

| 任务 | Job ID | 计划 | Agent | 主要产出 | 模型 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| Gateway Preflight - 早间健康预检 | `6b71e0af-…` | `30 6 * * *` | `main` | TG（仅异常） | (Local Script) |
| AI 应用监控 - 稳定源收集（Task 1） | `4ab47b1d-…` | `45 6 * * *` | `ai_app_monitor` | memory | `alibaba-cloud-bailian/qwen3.5-plus` |
| AI 应用开发监控日报（Task 2） | `63fd9053-…` | `15 7 * * *` | `ai_app_monitor` | TG + GH + memory | `alibaba-cloud-bailian/qwen3.5-plus` |
| AI 应用开发社交情报（Task 3） | `1212c84c-…` | `45 7 * * *` | `ai_app_monitor` | TG + memory | `alibaba-cloud-bailian/qwen3.5-plus` |
| AI Agent 每日精選（联动） | `65669bf4-…` | `15 8 * * *` | `ai_daily_pick` | TG + GH + memory | `alibaba-cloud-bailian/qwen3.5-plus` |
| Daily Watchdog - 验收与补跑 | `f0b3b711-…` | `5 9 * * *` | `ai_app_monitor` | TG（仅异常）+ memory | `alibaba-cloud-bailian/qwen3.5-plus` |

### 2.2 周度/多日任务

| 任务 | Job ID | 计划 | Agent | 主要产出 | 模型 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| AI 工作流灵感精选 | `3fc3b5c1-…` | `45 15 * * 1,3,5,0` | `reports` | TG + GH + memory | `alibaba-cloud-bailian/qwen3.5-plus` |
| **AI App Deep Dive** | `09050ca4-…` | `30 15 * * 2,4,6` | `reports` | TG + GH + memory | `alibaba-cloud-bailian/qwen3.5-plus` |
| AI App Weekly Deep Dive | `b4a7ac85-…` | `30 12 * * 0` | `reports` | TG + GH + memory | `alibaba-cloud-bailian/qwen3.5-plus` |

### 2.3 双周任务

| 任务 | Job ID | 计划 | Agent | 主要产出 | 模型 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **AI App Biweekly Report** | `dd10cad8-…` | `every 14d` | `reports` | TG + GH + memory | `alibaba-cloud-bailian/qwen3.5-plus` |
| **AI App Biweekly Reflection** | `a6f10d54-…` | `every 14d +30m` | `reports` | TG + GH | `alibaba-cloud-bailian/qwen3.5-plus` |

---

## 3. 质量规则（全任务适用）

### 3.1 时效性硬规则
- 优先近 72 小时，必要时放宽到 7 天。
- 超过 30 天默认不收录。
- 新信息不足时宁可少报，不凑数。

### 3.2 来源质量硬规则
- 每条关键结论至少 1 个一手来源（官方/GitHub/arXiv/会议/公告）。
- 二手来源只能补充，不能做唯一依据。
- 403/付费墙/仅摘要页面不能作为唯一依据。

---

## 4. 新增任务详情

### 4.1 AI App Deep Dive（P0，2026-02-15 新增）

对标 VLA Theory Deep Dive。每周二/四/六 15:30 各生成 1 篇深度分析文章（月产 ~12 篇）。

**架构**：prep-ai-deep-dive.py → Qwen Agent (autonomous) → post-ai-deep-dive.py

- **prep 脚本**（2026-02-15 rev.4）：读取最近 3 天 memory，按 importance + signal type 排序，去重检查，**输出 top 3 候选**（非 1 个），并对每个候选 URL 预抓取（GitHub API 优先、15s 超时、best-effort）。预抓取成功的排前面。预抓取内容存入 `memory/tmp/prefetch-{slug}.txt`。测试结果：29 条 → 3 候选，3/3 预抓取成功（GitHub API release notes），耗时 3.8 秒。
- **Qwen Agent**（prompt v2）：**先读本地预抓取文件**，内容足够则跳过 web_fetch；不足才触发 web_fetch 补充。失败时**自动切换下一候选**（非直接结束），最多尝试 3 个候选。撰写 1500-3000 字结构化文章。
- **post 脚本**：注入 YAML frontmatter (`auto_generated: true`)、LaTeX 安全网、写 memory、推 GitHub、发 Telegram。
- **GitHub 目标**：Agent-Playbook `blog/deep-dive/{slug}.md`
- **Memory**：`ai-app-deep-dive-articles.json`
- **Timeout**：1200s（20 分钟）

### 4.2 AI App Biweekly Report（P1，2026-02-15 新增）

对标 VLA-Handbook Biweekly Report。每 14 天生成一份跨期推理报告。

- **输入**：过去 14 天的 `ai-app-daily.json`、`ai-app-social-intel.json`、`ai-daily-pick.json`、`ai-app-workflow-digest.json`、`ai-weekly-digest.json`、`ai-app-deep-dive-articles.json`
- **输出**：500 字以内的跨期推理报告（模式/趋势/工具收敛/碎片化/可验证预测）
- **交付**：Telegram 2 条（报告 + GitHub 确认）+ GitHub Agent-Playbook `reports/biweekly/{date}.md`
- **Memory**：`ai-app-biweekly-reasoning.json`
- **GitHub 推送**：使用 `gh-contents-upload.py --config github-config-agent-playbook.json`
- **Timeout**：1200s（20 分钟）

### 4.3 AI App Biweekly Reflection（P1，2026-02-15 新增）

对标 VLA Biweekly Reflection。在 Biweekly Report 后 30 分钟运行。

- **输入**：`_ai_biweekly_{today}.md` + `ai-app-biweekly-reasoning.json` + `ai-app-deep-dive-articles.json`
- **输出**：3-10 个反思问题（趋势判断/资源分配/真伪判断/技术追问，至少 1 个技术追问）
- **交付**：Telegram + GitHub Agent-Playbook `reports/biweekly/reflection_{date}.md`
- **约束**：不给答案、锚定具体数据/工具、语气直接尖锐
- **Timeout**：600s（10 分钟）

### 4.4 AI 工作流灵感精选（P1，2026-02-18 prompt 升级）

每周一/三/五/日 15:45 运行。两阶段架构：Perplexity 搜索 → LLM 筛选整理。

**2026-02-18 视角升级**：从「如何构建 AI 应用的技巧集」升级为「AI 占领社会后哪些工作流已经/即将不可避免地改变」，面向更广泛的从业者（不限于工程师）。

**搜索词方向（`DEFAULT_SEARCH_TERMS`）**：
- 人机协作新范式：human AI collaboration workflow 10x productivity
- 认知外包：AI autonomous decision making replacing human judgment
- 常驻 AI：ambient AI always-on personal assistant workflow change
- 职业重构：future of work AI automation job role redesign
- 创作加速：solo creator AI workflow media content production
- 工程师视角：MCP agent workflow production adoption, agentic coding

**LLM 整理视角（`_build_llm_message`）**：
- 核心判断框架「必然性 Lens」—— 每条入选内容须能回答「这件事为什么是必然」
  - ① 认知外包：某类思考/判断被 AI 接管且效果更好
  - ② 规模跃迁：一人/小团队完成以前需部门规模才能做到的事
  - ③ 角色重构：某职业/流程正在被根本性重新定义
  - ④ 基础设施化：某 AI 能力从「先进工具」变为「行业标配」
  - ⑤ 人机边界移动：人类负责的部分单向减少、不可逆
- JSON 新增 `inevitability` 字段：一句话点明必然性
- 新 Tag 体系：`human-ai-collab`, `cognitive-outsourcing`, `role-shift`, `scale-leap`, `infra-ization`, `agentic-coding`, `creator-workflow`, `enterprise-adoption`, `decision-ai`, `cost-perf`
- Telegram 格式：🔮 必然性标签 → 标题 → 洞察 → 「为什么不可避免」→ 链接
- 收尾：「📌 本期共同信号」点出背后大趋势

---

## 5. Memory 与 GitHub

### 5.1 核心 memory 文件

| 文件 | 用途 |
| :--- | :--- |
| `ai-app-rss-YYYY-MM-DD.json` | Task 1 稳定源收集结果 |
| `ai-app-daily.json` | Task 2 日报结构化记录（`ai_app_daily[]` 按日累积，30 天窗口，via `memory-upsert.py`） |
| `ai-app-social-intel.json` | Task 3 社交情报记录（`social_intel[]` 按日累积） |
| `ai-daily-pick.json` | AI Pick 最终条目（`daily_picks[]` 按日累积，30 天窗口，via `memory-upsert.py`） |
| `ai-app-workflow-digest.json` | 工作流灵感精选 |
| `ai-weekly-digest.json` | Weekly Deep Dive 摘要 |
| `ai-app-deep-dive-articles.json` | Deep Dive 文章列表（去重用） |
| `ai-app-biweekly-reasoning.json` | Biweekly Report 推理记录 |
| `watchdog-log.json` | 每日验收日志 |
| `assumptions.json` | 核心假设登记簿（14 条，跨 VLA/AI Agent/投资），Calibration Check 读取 |
| `calibration-check-{date}.json` | 每日假设校准扫描结果（Calibration Check 产出） |

### 5.2 GitHub 落盘（Agent-Playbook 仓库）

| 路径 | 来源 |
| :--- | :--- |
| `cognition/app_index.md` | 监控日报 |
| `memory/blog/archives/ai-daily-pick/YYYY-MM-DD.md` | 每日精选 |
| `memory/blog/archives/ai-workflow-inspiration/YYYY-MM-DD.md` | 工作流灵感精选 |
| `memory/blog/archives/deep-dive/{slug}.md` | AI App Deep Dive 文章 |
| `memory/blog/archives/2026/{date}-weekly-digest.md` | Weekly Deep Dive |
| `reports/biweekly/{date}.md` | Biweekly Report |
| `reports/biweekly/reflection_{date}.md` | Biweekly Reflection |

---

## 6. 路由与通知

系统共有 3 个 Telegram Bot，按内容领域严格分账号路由（**`payload.account` 字段**）：

| account | Bot 名称 | 适用任务 |
| :--- | :--- | :--- |
| `original` | VLA-handbook-moltbotDaily | 所有 VLA 任务、系統自省与自動調參 |
| `ai_agent_dailybot` | AI_agent_dailybot | 所有 AI App / AI Agent 任务 |
| `new` | direct-chat | 纯运维告警（Watchdog、Preflight、Server Health Check） |

**重要**：新建 cron job 时 `account` 默认为空（走 `original` = VLA bot）。AI App 类任务**必须显式设置** `account: ai_agent_dailybot`，否则内容会发到 VLA 频道。

- 所有任务的目标 chat id：`1898430254`（同一用户 ID，通过不同 bot 区分频道）
- Telegram `streamMode=off`，避免过程文本泄漏。
- `deliver=false` 的任务由 post 脚本负责 Telegram 发送，post 脚本内同样须指定正确 account。

---

## 7. 运维机制

### Gateway Preflight
- 每天 `06:30` 运行：先测 gateway，可用则静默；不可用则自动重启并复检。
- 脚本：`/home/admin/clawd/scripts/gateway-preflight.py`
- `CRITICAL_JOB_IDS` 守护包含所有 AI App 关键 job。

### Daily Watchdog
- 每天 `09:30` 统一验收：检查 memory/GitHub 是否落盘。
- `memory_rw` 读写回环检查，验证 memory 系统 functional。
- 缺失时自动触发补跑；成功时完全静默。

---

## 8. 变更日志

### 2026-02-19 rev.12

- **全系统 post script account argparse default 加固**：
  - **根因**：rev.11 修复了 cron job `payload.account` 的路由错误，但 post script 本身的 argparse `--account` 默认值仍为空字串。当 `args.account=""` 时，`_send_telegram(account="")` → `if account:` 为 False → 不传 `--account` 给 moltbot → 使用全局 botToken（VLA bot）。对 AI App 类 post script，这等于路由回了 VLA bot——与 rev.11 修复的问题完全相同，只是触发路径不同（直接调用 post script，而非 cron job 路由错误）。
  - **修复清单**：
    - `post-ai-app-workflow.py`：`default=""` → `default="ai_agent_dailybot"`（**真实 bug**：run script 正常传参时无问题，但 prompt/standalone 调用时会路由到 VLA bot）
    - `post-ai-app-social.py`：`default=""` → `default="ai_agent_dailybot"`（rev.3 前已修，此处补充记录）
    - `post-vla-daily/theory/sota/release/weekly.py`：`default=""` → `default="original"`（VLA 任务，使用全局 botToken 结果正确，但改为显式更安全）
  - **所有 AI App post scripts 的 account default 状态（2026-02-19 审计后）**：
    - `post-ai-deep-dive.py`：`ai_agent_dailybot` ✓（rev.11 修复）
    - `post-ai-app-social.py`：`ai_agent_dailybot` ✓（rev.12 修复）
    - `post-ai-app-workflow.py`：`ai_agent_dailybot` ✓（rev.12 修复）
    - `post-ai-weekly.py`：未修改（run script 默认 `ai_agent_dailybot` 并传入）

- **root ownership 修复**：
  - **根因**：Cursor IDE 所有文件工具（StrReplace/Write）以 root 用户运行。过去数次修改 `/home/admin/clawd/scripts/` 下的脚本，导致 21 个文件 owner 变成 root:root。
  - **影响**：脚本执行正常（world-readable），但 `sudo -u admin` 无法修改这些文件，给未来维护造成陷阱。
  - **修复**：`sudo chown admin:admin /home/admin/clawd/scripts/*.py`，恢复所有脚本 owner 为 admin。
  - **预防**：以后需要修改这些脚本时，**可以使用 Cursor StrReplace**（root 可修改 admin-owned 文件），修改后建议确认 owner 正确。若用 `sudo -u admin` 方式修改，则文件必须已是 admin-owned。

### 2026-02-19 rev.11

- **全系统 Telegram account 路由修复（系统性问题）**：
  - **现象**：用户发现 `AI App Deep Dive` 推送到了 VLA Telegram 频道（而非 AI App 频道）。
  - **根因**：Moltbot cron job 的 `payload.account` 默认为空，等价于走 `original` bot（VLA-handbook-moltbotDaily）。所有 AI App 任务在创建时均未显式指定 `ai_agent_dailybot`，导致长期发错频道。
  - **排查**：全面盘点 32 个 job 的 `account` 字段，发现 7 个任务路由错误。
  - **修复方式**：停止 gateway → 以 admin 身份批量更新 `jobs.json` → 重启 gateway（`moltbot cron edit` CLI 不支持 `--account` 选项，只能直接修改持久化文件）。
  - **修复清单（共 7 个）**：

    | 任务 | 原 account | 修复后 |
    | :--- | :--- | :--- |
    | AI App Deep Dive | `original` | `ai_agent_dailybot`（+ deliver=false，post 脚本默认值同步修复） |
    | AI 应用开发监控日报 | `original` | `ai_agent_dailybot` |
    | AI Agent 每日精選 | `original` | `ai_agent_dailybot` |
    | AI Agent 自省与自动调参 | `original` | `ai_agent_dailybot` |
    | AI App Biweekly Report | `original` | `ai_agent_dailybot` |
    | AI App Biweekly Reflection | `original` | `ai_agent_dailybot` |
    | Server Health Check - Fixed | `original` | `new` |

  - **确认正确（无需修改）**：VLA 系列、系統自省與自動調參 → `original`；Gateway Preflight、Daily Watchdog → `new`。
  - **操作规范更新**：见 §6「路由与通知」，新建任务时必须显式指定 `account`。

- **VLA Daily Hotspots — 两阶段脚本从未创建（已修复）**：
  - `run-vla-daily-two-phase.py` 从未存在，任务每日静默失败（lastStatus=ok 但 Telegram 无推送）。
  - 新建脚本：读取 `vla-rss-YYYY-MM-DD.json` → LLM 选出 5-10 篇热点 → `post-vla-daily.py` 落盘推送。
  - 补跑成功：7 篇论文入库，paper_index.md 更新，Telegram 发送（Message ID: 430）。
  - 教训：`ok status ≠ 任务成功`；验收须同时检查 runs summary + Telegram + memory 文件日期。

---

### 2026-02-18 rev.10

- **AI 工作流灵感精选 — 视角全面升级**：
  - **旧定位**：「怎么构建 AI 应用」的技巧精选，面向工程师。
  - **新定位**：「AI 大规模占领社会后，哪些工作流已经/即将不可避免地改变」，面向工程师、产品、创作者、管理者。
  - **修改点 1（`prep-ai-app-workflow.py`）**：`DEFAULT_SEARCH_TERMS` 替换为 10 条以「人机协作 / 认知外包 / 职业重构 / 规模跃迁 / 常驻 AI / 创作加速 / 基础设施化」为主轴的搜索词。
  - **修改点 2（`run-ai-app-workflow-two-phase.py`）**：`_build_llm_message()` 整体重写：
    - 引入「必然性 Lens（Inevitability Lens）」判断框架（5 个信号维度）
    - JSON schema 新增 `inevitability` 字段（一句话点明不可逆性）
    - Tag 体系从技术类（agent-arch, rag 等）升级为趋势类（cognitive-outsourcing, role-shift, scale-leap 等）
    - Telegram 格式：🔮 必然性标签 + 「→ 为什么不可避免」结构，收尾「本期共同信号」
    - 读者画像：非工程师也能读完说「我的工作要变了」即为合格

---

### 2026-02-18 rev.9

- **AI App Biweekly Report — Gateway 首次运行 Race Condition（已补跑）**：
  - **现象**：`AI App Biweekly Report`（anchorMs = 2026-02-18 10:00 CST，首次运行）在 Gateway 重启时触发，`nextRunAtMs` 被推进到 3/4，但 **session 未创建，报告文件不存在**。
  - **补跑方式**：设置 `jobs.json state.nextRunAtMs` 为过去时间，Gateway 在下一次首次运行检测中拾起任务执行。
  - **产出**：`/home/admin/clawd/memory/_ai_biweekly_2026-02-18.md`（500 字内），GitHub `memory/reports/2026-02-18.md`，Telegram 2 条，`ai-app-biweekly-reasoning.json` 已更新。
  - **注意**：VLA 侧同类双周任务（`VLA-Handbook Biweekly Report`、`VLA Biweekly Reflection`）亦为首次，需关注触发时是否出现相同问题。

- **AI App Biweekly Reflection — skip 分支 Telegram 发送错误修复**：
  - **现象**：Reflection 找不到当日报告文件，进入 skip 分支，LLM 尝试两次工具发送均失败，每次失败均以 `⚠️ ✉️ Message: send failed` 出现在 Telegram。
  - **修复**：将 prompt skip 指令从"投递 Telegram"改为"输出最终回复（deliver=true 自动投递）"，禁止 LLM 使用 `message` 工具发送。
  - **下次正常运行**：2026-03-04 10:30 CST（使用修复后 prompt）。

---

### 2026-02-18 rev.8

- **AI Agent 每日精選 — memory 写入格式根治**：
  - Prompt Section 6 与末尾规则存在矛盾，LLM 跟随靠前的 Section 6 使用旧格式 `{date, items}` 直接覆写，导致历史记录每次被清空。
  - 修复：Section 6 改为 `echo PAYLOAD | memory-upsert.py --key daily_picks`，末尾重复规则块删除。
  - 今日产出的旧格式文件已手动转换为 `{daily_picks: [{date, items}]}` 正确结构。
  - 明天起 `ai-daily-pick.json` 将正确累积 30 天历史，Deep Dive 和 Calibration Check 两个下游均直接兼容。

### 2026-02-18 rev.7

- **Agent-Playbook 目录迁移**：
  - GitHub 输出路径全面更新（`blog/` → `memory/blog/archives/`，`stack/` → `cognition/`）。
  - AI App Weekly Deep Dive 调度更新：`0 11 * * 0` → `30 12 * * 0`（周日 12:30，与评审/自省错峰）。
- **新增跨域任务引用**：
  - **Calibration Check**（09:15）读取 AI App 侧 3 个 memory 源（`ai-app-daily.json`、`ai-daily-pick.json`、`ai-app-social-intel.json`），作为假设校准扫描的输入之一。
  - 新增 `assumptions.json`（假设登记簿）和 `calibration-check-{date}.json`（扫描产出）到 memory 文件清单。

### 2026-02-15 rev.6

- **主力模型全面迁移至 Qwen3.5-Plus**：
  - 将所有任务模型切换为 `alibaba-cloud-bailian/qwen3.5-plus`。
  - **全量开启 `enable_search`**：通过 `moltbot.json` 全局配置，模型自主决定联网时机。
- **Memory 累积写入机制改造**：
  - 问题：AI 应用开发监控日报和 AI Agent 每日精選由 agent 直接 `write_file` 写 memory，每次覆盖导致历史丢失（5 天运行只剩 1 天数据）。
  - 新增 `memory-upsert.py` 通用脚本：stdin JSON → 按 date upsert → 30 天窗口自动清理 → 原子写回。
  - `ai-app-daily.json` prompt 改为 pipe 给 `memory-upsert.py --key ai_app_daily`。
  - `ai-daily-pick.json` 结构从 `{date, items}` 迁移为 `{daily_picks: [{date, items}]}`，prompt 改为 pipe 给 `memory-upsert.py --key daily_picks`。
  - 适配脚本：`prep-ai-deep-dive.py`、`prep-ai-weekly.py`、`daily-watchdog.py` 已更新为兼容新旧结构。
- **`enable_search` 范围收紧**：
  - 移除 6 个纯脚本执行任务的 `enable_search`（VLA Social Intel、AI 社交情报、SOTA Tracker、Release Tracker、VLA Weekly、AI Weekly）。
  - 仅保留 3 个真正需要搜索的 agent 研究任务（VLA Theory Deep Dive Round 1/2、AI App Deep Dive）。
  - 原因：`enable_search` 使 agent 对纯脚本任务的指令服从性下降（实测 agent 自行添加 `--debug` 参数导致脚本失败）。

### 2026-02-15 rev.5

- **启用百煉 `enable_search` 联网搜索（AI App 相关 3 个任务）**：
  - AI App Deep Dive、AI App Weekly Deep Dive、AI 应用开发社交情报 的 cron payload 新增 `streamParams: {enable_search: true, search_strategy: "agent"}`。
  - 修改了 Moltbot 源码（`extra-params.js`、`cron.js` schema、`isolated-agent/run.js`），支持通过 cron payload 透传 API 级别的搜索参数。
  - `search_strategy: "agent"` 是阿里百煉为 Qwen3-Max 优化的复杂检索模式，在 agent 推理过程中自主决定何时搜索。
  - 配合已有的 `web_fetch` 工具和 URL 预抓取机制，形成三层信息获取防线：L1 本地预抓取 → L2 web_fetch 补充 → L3 enable_search 兜底。

### 2026-02-15 rev.4

- **prep-ai-deep-dive.py 加入 URL 预抓取 + 多候选兜底**：
  - 默认输出 top 3 候选（`--max 3`），而非 1 个。Agent 按顺序尝试，失败自动切下一个。
  - 新增 `_prefetch_candidate()` 函数：对每个候选 URL 预抓取（15s 超时，best-effort）。GitHub URL 优先走 API（`/repos/.../releases?per_page=3`），返回干净的 release notes JSON 而非 SSR HTML。其他 URL 走 HTML → strip tags。
  - 预抓取内容存入 `memory/tmp/prefetch-{slug}.txt`（最大 8000 chars）。
  - Candidate JSON 新增字段：`prefetch_ok`、`prefetch_snippet`（2000 chars 预览）、`prefetch_snippet_file`、`prefetch_url_used`。
  - 排序：`prefetch_ok=true` 排前面 → agent 首选可用内容的候选。
- **Deep Dive agent prompt v2**：步骤 2 分为 2a（读本地预抓取）和 2b（web_fetch 补充），失败时循环候选而非直接结束。

### 2026-02-15 rev.3

- **prep-ai-deep-dive.py 字段映射修复（致命级）**：完全重写 3 个 extract 函数对齐真实 memory 结构。
- **端到端验证通过**：prep → post → gh-contents-upload → gateway-preflight → biweekly prompt 全部一致。

### 2026-02-15 rev.2

- **AI App Deep Dive（新增）**：周二/四/六 15:30，自主 agent 生成深度分析文章。prep-ai-deep-dive.py + post-ai-deep-dive.py。Job ID: `09050ca4-6ed2-4b4f-9cf4-9f51d3225132`。
- **AI App Biweekly Report（新增）**：每 14 天，anchor Feb 17 10:00 CST。跨期推理报告，500 字以内。Job ID: `dd10cad8-dd66-44bf-bbbe-b9bd6498792d`。
- **AI App Biweekly Reflection（新增）**：每 14 天，Biweekly Report 后 30 分钟。3-10 个反思问题。Job ID: `a6f10d54-2b67-430a-a199-f296c5c71f49`。
- 3 个新 job 全部加入 `gateway-preflight.py` CRITICAL_JOB_IDS 守护。

### 2026-02-14

- payload.kind 修复、isolation 配置修复、AI 工作流 GitHub 推送等变更。
- Gateway Preflight 新增配置校验。
- 多个两阶段任务统一补设 timeoutMs。

---

## 7) 任务 Prompt 设计一览

### 7.1 AI App 监控任务 (11 个)

#### AI 应用监控 - 稳定源收集
- **Prompt 類型**：純腳本 wrapper
- **核心目的**：每日清晨採集 AI 應用開發領域的穩定 RSS 源數據。
- **關鍵設計決策**：
  - **極簡模式**：採用單次 exec 調用，確保採集流程穩定且不被 LLM 幻覺干擾。
- **硬約束**：禁止 web_search/fetch；禁止添加解釋文字。

#### AI 应用开发监控日报
- **Prompt 類型**：自主 Agent
- **核心目的**：根據採集到的 RSS 生成每日監控日報，存入 memory 與 GitHub。
- **關鍵設計決策**：
  - **嚴格時效**：優先 72 小時內信息，超過 30 天默認不收錄。
  - **來源優先**：強制要求一手來源（官方、GitHub Release 等），二手轉述僅作補充。
  - **寧缺毋濫**：新信息不足時減少篇幅，不湊數。
- **硬約束**：必須包含一手來源鏈接；禁止模糊引用。

#### AI 应用开发社交情报
- **Prompt 類型**：兩阶段 wrapper
- **核心目的**：監控 AI 應用開發領域的 Twitter、GitHub Trending 社交動態。
- **關鍵設計決策**：
  - **複雜邏輯封裝**：依賴 `run-ai-app-social-two-phase.py` 處理網頁抓取與清洗雜訊。
- **硬約束**：僅返回腳本 stdout 作為唯一回覆。

#### AI Agent 每日精選
- **Prompt 類型**：自主 Agent
- **核心目的**：跨 VLA 與 AI App 兩個頻道，挑選當日最具價值的單一核心亮點。
- **關鍵設計決策**：
  - **高門檻篩選**：強調實證（GitHub Release、官方博客）而非媒體轉述。
  - **歷史標註**：引用超過 7 天前的信息必須標註「舊聞補充」。
- **硬約束**：禁止占位符或模糊來源；寧缺毋濫。

#### AI 工作流灵感精选
- **Prompt 類型**：兩阶段 wrapper
- **核心目的**：每週四次（一/三/五/日）從採集數據中提取 AI 工作流（Workflow）相關靈感。
- **關鍵設計決策**：
  - **兩階段執行**：由 `run-ai-app-workflow-two-phase.py` 處理數據過濾。
- **硬約束**：僅返回腳本 stdout。

#### AI App Deep Dive
- **Prompt 類型**：自主 Agent
- **核心目的**：針對最具潛力的 AI 工具或產品進行 1500-3000 字的深度文章分析。
- **關鍵設計決策**：
  - **預抓取優先**：優先讀取 prep 腳本提供的預抓取文件，內容不足才調用 web_fetch。
  - **備選機制**：當首選目標抓取失敗時，自動嘗試下一候選（最多 3 個）。
- **硬約束**：web_fetch 總計 <= 5 次；嚴禁編造版本號或數字。

#### AI App Weekly Deep Dive
- **Prompt 類型**：兩阶段 wrapper
- **核心目的**：每週日對本週 AI 應用開發領域的所有進展進行綜述與歸檔。
- **關鍵設計決策**：
  - **數據聚合**：調用 `run-ai-weekly-two-phase.py` 整合整週 memory 數據。
- **硬約束**：僅返回腳本 stdout。

#### AI App Biweekly Report
- **Prompt 類型**：自主 Agent
- **核心目的**：每兩週進行一次 AI 應用領域的跨內容推理與趨勢識別。
- **關鍵設計決策**：
  - **長期視角**：強調工具收斂、碎片化趨勢等跨期推理。
- **硬約束**：正文限 500 字以內；空 Section 刪除。

#### AI App Biweekly Reflection
- **Prompt 類型**：自主 Agent
- **核心目的**：生成 3-10 個反思問題，引發讀者對 AI 應用趨勢的深層判斷。
- **關鍵設計決策**：
  - **題型覆蓋**：強制包含趨勢判斷、資源分配、真偽判斷、技術追問四類。
- **硬約束**：問題必須錨定具體數據/工具/事件。

#### AI Agent 自省与自动调参 (J-AI)
- **Prompt 類型**：自主 Agent
- **核心目的**：AI Agent 監控管道的進化引擎，自動優化配置。
- **關鍵設計決策**：
  - **閉環邏輯**：觀察近 7 天數據 → 執行低風險改動 → 驗證改動效果。
  - **數據門檻**：若當週數據量不足（<4天），則進入保守調整模式。
- **硬約束**：每輪最多執行 3 個低風險改動。

#### Token Usage Weekly Report
- **Prompt 類型**：自主 Agent（跨任務統計）
- **核心目的**：每週六統計系統所有任務的 Token 消耗、費用與環比變化。
- **關鍵設計決策**：
  - **動態掃描**：實時通過 `cron list` 獲取當前任務列表，而非硬編碼。
  - **多維分析**：統計次數、總 Token、平均成本及手動對話成本。
- **硬約束**：必須與上週數據對比計算環比變化。

---

### 7.2 共享运维任务 (5 个)

#### Gateway Preflight
- **Prompt 類型**：純腳本 wrapper
- **核心目的**：每日清晨重啟網關、校驗 `jobs.json` 配置並檢測 Root 權限污染。
- **關鍵設計決策**：
  - **靜默原則**：stdout 為空則代表正常，Agent 立即結束且不發送回覆。
- **硬約束**：禁止調用任何工具（除 1 次 exec）；禁止輸出過程文本。

#### Daily Watchdog
- **Prompt 類型**：純腳本 wrapper
- **核心目的**：上午驗證當日所有核心任務的產出，並在缺失時觸發補跑。
- **關鍵設計決策**：
  - **噪聲過濾**：Agent 需過濾 `[progress]` 雜訊，僅返回正式報告內容。
- **硬約束**：過濾後若為空，則最終回覆為空。

#### System Health Snapshot
- **Prompt 類型**：純腳本 wrapper
- **核心目的**：生成系統當日健康快照，過濾 CLI 偽失敗，存入 `system-health.json`。
- **關鍵設計決策**：
  - **數據依賴**：其產出的 JSON 為每週質量評審提供核心量化依據。
- **硬約束**：僅返回腳本 stdout。

#### Memory Janitor
- **Prompt 類型**：純腳本 wrapper
- **核心目的**：每日掃描內存文件夾，報告建議清理的項（30d/14d/3d 窗口）。
- **關鍵設計決策**：
  - **安全護欄**：目前僅運行 `--dry-run` 模式，只報告不執行實際刪除。
- **硬約束**：僅返回腳本 stdout。

#### Server Health Check - Fixed
- **Prompt 類型**：純腳本 wrapper
- **核心目的**：每小時監控磁盤、內存與 Gateway 進程狀態。
- **關鍵設計決策**：
  - **結構化告警**：⚠️ 告警頭 + 腳本輸出內容 + 針對性運維建議。
- **硬約束**：禁止過程性敘述；stdout 為空則回覆為空。

---

### 7.3 系统进化任务 (4 个)

#### Weekly Output Quality Review
- **Prompt 類型**：自主 Agent
- **核心目的**：每週日評審過去 7 天的產出質量，識別漏報（Missed Stories）。
- **關鍵設計決策**：
  - **雙域分欄**：嚴格區分 VLA 與 AI Agent 域，禁止將兩域結論混寫。
  - **量化校準**：讀取 `system-health.json` 進行數據對齊。
- **硬約束**：每條 missed story 或建議必須標註 scope（vla / ai_agent）。

#### Monthly Prompt Evolution Review
- **Prompt 類型**：自主 Agent
- **核心目的**：每月 1 日分析質量趨勢，提出 Prompt 改進提案與可驗證指標。
- **關鍵設計決策**：
  - **權責分離**：Agent 僅提供建議與指標，不進行自動化的配置修改。
  - **假設登記簿維護（三步）**：① 過期 horizon 掃描 → ② 累積觸發審視 → ③ 覆蓋盲區候選假設發現。
  - **候選假設發現**：掃描近 30 天 memory 信號，VLA 域與 AI Agent 域各自獨立識別「現有假設未覆蓋的結構性規律」。每個候選必須包含：現象 + memory 文件名 + 近 30 天次數、與現有假設的差異說明、hypothesis 草案、成熟度（emerging / accumulating / mature）。
  - **A/B 雙欄強制輸出**：【A 現有假設調整】（修參數/措辭）與【B 候選假設（全新）】必須分欄列出，不得混寫。
- **硬約束**：必須雙域分欄評價；候選假設僅在成熟度 accumulating（3–5 次信號）及以上才提出，信號不足可空缺並注明原因；修改 `assumptions.json` 須人工確認。

#### Calibration Check - 假设校准扫描
- **Prompt 類型**：自主 Agent
- **核心目的**：每日掃描核心假設是否觸發失效條件。
- **關鍵設計決策**：
  - **跨域掃描**：整合 VLA、AI Agent 與投資等多個維度的信號進行碰撞。
  - **異常警報**：無信號則靜默；有觸發則 Telegram 告警。
- **硬約束**：修改 `assumptions.json` 必須人工確認。

#### 系統自省與自動調參 (J)
- **Prompt 類型**：自主 Agent
- **核心目的**：VLA 管道的進化引擎，自動優化關鍵詞配置。
- **關鍵設計決策**：
  - **閉環邏輯**：觀察過去 14 天數據 → 執行低風險改動 → 下一週期驗證。
- **硬約束**：每輪最多自動執行 3 個改動。
