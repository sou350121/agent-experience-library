---
auto_generated: true
generated_at: "2026-02-26T01:43:55Z"
source_url: "https://kanyilmaz.me/2026/02/23/cli-vs-mcp.html"
signal_type: "blog_post"
---
# MCP 成本优化：用 CLI 方案降低 94% Token 开销 (Making MCP 94% Cheaper via CLI)

> 🔍 本文由 Moltbot 自动生成 | 2026-02-26
>
> **项目/工具**: CLIHub + MCP CLI 方案
> **链接**: https://kanyilmaz.me/2026/02/23/cli-vs-mcp.html
> **核心定位**: 通过将 MCP 工具目录从「全量预加载」改为「按需懒加载」，实现 94% 的 token 成本节省

## 一句话摘要

将 MCP 工具调用从 JSON Schema 全量预加载改为 CLI 懒加载模式，在典型场景（6 个 MCP 服务器、84 个工具）下可节省 94% 的 token 开销，且兼容任意模型（不限于 Anthropic）。

## 是什么 / 解决什么问题

MCP（Model Context Protocol）是当前 AI Agent 调用外部工具的主流协议。但它的默认实现有一个隐蔽的成本问题：**每次会话启动时，MCP 会把所有工具的完整 JSON Schema  dump 到对话上下文中**。

这意味着什么？假设你有 6 个 MCP 服务器，每个提供 14 个工具，总共 84 个工具。每个工具的 schema 平均约 185 tokens，那么：

- **会话启动成本**: 185 × 84 = 15,540 tokens
- **这还只是开始**——每次工具调用还需要额外的协议开销

这个问题在以下场景尤其严重：
- Agent 需要频繁切换工具（如同时操作 Notion、Linear、GitHub）
- 长会话中工具目录多次重载
- 使用高价模型（如 Claude Opus）时，token 成本直接转化为金钱成本

CLI 方案的核心思路：**用「轻量级技能列表」替代「全量 schema 预加载」，工具详情在首次调用时按需获取**。

## 技术架构拆解

### 架构本质

这个方案的核心不是「用 CLI 替代 MCP」，而是**改变信息加载的时机和粒度**：

- **MCP 默认方案**：假设「Agent 需要预先知道所有工具的完整结构」→ 启动时全量加载
- **CLI 方案**：假设「Agent 只需要知道工具名称，细节可以按需获取」→ 启动时轻量加载 + 运行时懒加载

这种思路转变带来的收益是指数级的：工具数量从 10 增长到 100 时，MCP 的启动成本线性增长（+15,000 tokens），而 CLI 的启动成本几乎不变（仍为 ~300 tokens）。

### 核心设计决策

| 设计点 | MCP 默认方案 | CLI 方案 |
|--------|-------------|---------|
| 会话启动加载内容 | 所有工具的完整 JSON Schema | 仅工具名称 + 描述 + 路径（~50 tokens/工具） |
| 工具详情获取时机 | 启动时一次性加载 | 首次调用前通过 `--help` 获取 |
| 工具调用格式 | JSON-RPC 风格 `{"tool_call": {...}}` | Shell 命令风格 `$ tool subcommand args` |
| 模型兼容性 | 任意 | 任意（不依赖特定平台 API） |

### 理论好用，实际呢？

**理论上的收益**很清晰：94% token 节省，适用于任意模型，实现门槛不高。

**实际落地时的陷阱**：

1. **--help 输出质量参差不齐**
   - 有些 CLI 的 `--help` 输出很简略，缺少参数类型和约束信息
   - Agent 可能需要多次试错才能找到正确的命令格式
   - 解决：用 CLIHub 生成 CLI 时可以自定义 `--help` 模板，确保输出完整

2. **工具发现延迟**
   - MCP 方案中，Agent 启动时就知道所有可用工具
   - CLI 方案中，Agent 需要执行 `--help` 后才能知道具体子命令
   - 对于复杂工作流（需要组合多个子命令），可能增加 1-2 轮交互

3. **错误处理更复杂**
   - MCP 的 JSON Schema 提供类型验证，错误在调用前就能发现
   - CLI 的错误通常在执行后才暴露（如参数类型错误、缺少必填参数）
   - 解决：在 Agent 系统提示中明确要求「先 `--help` 再执行」

4. **状态管理问题**
   - 如果 Agent 忘记已经执行过 `--help`，可能重复获取命令参考
   - 需要在 Agent 的记忆机制中记录「已探索的工具」
   - 参考：Openclaw 的 `available_skills` 格式包含工具位置，Agent 可以缓存已读取的帮助信息

### 与前版/竞品的关键差异

| 维度 | MCP 默认方案 | Anthropic Tool Search | CLI 方案 |
|------|-------------|----------------------|---------|
| 会话启动开销 | ~15,540 tokens (84 工具) | ~500 tokens | ~300 tokens |
| 首次工具调用总成本 | ~15,570 tokens | ~3,530 tokens | ~910 tokens |
| 10 次工具调用总成本 | ~15,840 tokens | ~3,800 tokens | ~964 tokens |
| 100 次工具调用总成本 | ~18,540 tokens | ~12,500 tokens | ~1,504 tokens |
| 节省比例（vs MCP） | - | 85% | 94% |
| 节省比例（vs Tool Search） | - | - | 74-88% |
| 平台依赖 | 无 | 仅 Anthropic | 无 |
| 实现复杂度 | 低 | 中（需集成 Tool Search API） | 中（需生成 CLI） |

### 架构/信息流图

```
┌─────────────────────────────────────────────────────────────────┐
│                        MCP 默认方案                              │
├─────────────────────────────────────────────────────────────────┤
│  Session Start                                                  │
│  ↓                                                              │
│  [加载全部 84 个工具 schema] → +15,540 tokens                    │
│  ↓                                                              │
│  Agent 执行任务                                                  │
│  ↓                                                              │
│  [工具调用] → +30 tokens/次                                     │
│                                                                 │
│  总成本 (10 次调用): ~15,840 tokens                              │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                        CLI 方案                                  │
├─────────────────────────────────────────────────────────────────┤
│  Session Start                                                  │
│  ↓                                                              │
│  [加载轻量技能列表] → +300 tokens                                │
│  ↓                                                              │
│  Agent 执行任务                                                  │
│  ↓                                                              │
│  [首次调用某工具]                                                │
│    → $ tool --help → +600 tokens (获取该工具全部子命令)          │
│    → $ tool subcommand args → +6 tokens (实际执行)              │
│  ↓                                                              │
│  [后续调用同一工具]                                              │
│    → 直接使用已缓存的命令格式 → +6 tokens/次                     │
│                                                                 │
│  总成本 (10 次调用): ~964 tokens                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Token 成本对比表

| 场景 | MCP | CLI | 节省 |
|------|-----|-----|------|
| 会话启动 | 15,540 | 300 | 98% |
| 1 次工具调用 | 15,570 | 910 | 94% |
| 10 次工具调用 | 15,840 | 964 | 94% |
| 100 次工具调用 | 18,540 | 1,504 | 92% |

> 注：以上数据基于 6 个 MCP 服务器、84 个工具的典型配置。CLI 方案假设每个工具首次调用需 `--help` 获取命令参考（~600 tokens），后续调用仅需命令本身（~6 tokens）。

## 实用评估

### 什么场景值得用

1. **多工具协作场景**
   - Agent 需要同时操作多个服务（Notion + Linear + GitHub + Slack）
   - 每个服务的 MCP 服务器提供 10+ 个工具
   - 节省效果随工具数量线性增长

2. **长会话/高频调用场景**
   - 单次会话持续数小时，工具调用次数 > 50
   - 如：自动化工作流、持续集成 Agent、研究助手

3. **成本敏感场景**
   - 使用高价模型（Claude Opus、GPT-4）
   - 大规模部署（数十/数百个 Agent 实例）
   - Token 成本直接转化为运营支出

4. **跨模型兼容需求**
   - 需要在不同模型提供商之间切换（Anthropic → OpenAI → 本地模型）
   - Tool Search 等方案绑定特定平台，CLI 方案无此限制

### 什么场景不值得用

1. **工具数量极少（< 5 个）**
   - 启动开销本身很小，优化收益有限
   - 增加 CLI 生成和维护成本可能不划算

2. **工具调用频率极低**
   - 如：每天仅调用 1-2 次
   - 懒加载的 `--help` 开销占比过高

3. **工具 schema 动态变化频繁**
   - 如果工具参数经常变动，CLI 需要重新生成
   - MCP 的运行时 schema 发现更适合动态场景

4. **需要严格类型检查的场景**
   - JSON Schema 提供完整的类型约束和验证
   - CLI 方案的 `--help` 输出是纯文本，类型信息可能丢失

### 迁移成本

**从 MCP 迁移到 CLI 方案：**

1. **安装 CLIHub**（一次性）
   ```bash
   # CLIHub 是开源的 MCP→CLI 转换器
   # https://github.com/thellimist/clihub
   ```

2. **为每个 MCP 服务器生成 CLI**
   ```bash
   # 示例：为 Notion MCP 生成 CLI
   clihub generate --mcp-server notion-mcp --output ~/bin/notion
   ```

3. **更新 Agent 系统提示**
   - 将 `available_tools` 从 JSON Schema 改为 CLI 风格描述
   - 参考 Openclaw 的 [available_skills block 格式](https://github.com/openclaw/openclaw/blob/9363c320d8ffe29290906752fab92621da02c3f7/docs/concepts/system-prompt.md?plain=1#L102-L120)

4. **测试工具调用流程**
   - 验证 `--help` 输出是否包含足够的参数信息
   - 确保 Agent 能正确解析命令格式

**预估工作量：**
- 小型项目（1-3 个 MCP 服务器）：1-2 小时
- 中型项目（4-10 个 MCP 服务器）：半天到一天
- 大型项目（10+ 个 MCP 服务器）：1-2 天

## Claude Code 盲点与陷阱

如果你用 Claude Code 或类似 Agent 直接操作 CLI，注意这些坑：

### ❌ 错误做法：跳过 --help 直接执行

```bash
# Claude 可能这样「猜」命令格式
$ notion search "roadmap" --filter object=page
# 错误：参数格式不对，实际应该是 --filter-property object --filter-value page
```

**为什么会出现**：Claude 看到工具名称后，可能基于常识推断命令格式，而不是先读取 `--help`。

**正确做法**：在系统提示中明确要求：
```
使用任何 CLI 工具前，必须先执行 $ tool --help 获取完整的命令参考。
不要猜测参数名称或格式。
```

### ❌ 错误做法：忽略 --help 的输出长度

```bash
# 某个复杂工具的 --help 可能输出 2000+ tokens
$ complex-tool --help
# Claude 可能只读取前几行就尝试执行，遗漏关键参数
```

**解决**：在系统提示中说明「等待完整输出后再解析」。

### ✅ 正确做法示例

```bash
# 步骤 1：获取帮助（首次调用某工具时）
$ notion --help
# 等待完整输出，解析可用子命令和参数

# 步骤 2：执行命令
$ notion search "Q1 roadmap" --filter-property object --filter-value page

# 步骤 3：记录该工具的命令格式（供后续调用复用）
```

## 并发与安全考量

### 并发场景下的行为

当多个 Agent 实例同时使用 CLI 方案时：

| 场景 | MCP 方案 | CLI 方案 |
|------|---------|---------|
| 工具调用并发 | 通过 MCP 服务器统一调度 | 直接执行本地命令，需注意文件锁 |
| OAuth 令牌管理 | MCP 服务器集中管理 | 每个 CLI 实例需独立处理令牌刷新 |
| 速率限制 | MCP 服务器可统一限流 | 需在 CLI 层或 Agent 层实现限流 |

**建议**：
- 如果多个 Agent 共享同一个 CLI 二进制，确保 OAuth 令牌存储是进程安全的
- CLIHub 生成的 CLI 默认使用 `~/.clihub/tokens/` 目录存储令牌，需确保文件锁机制

### 安全注意事项

1. **命令注入风险**
   - CLI 方案的参数直接传递给 shell，需防范注入攻击
   - 示例：用户输入 `"; rm -rf /` 可能被直接执行
   - 解决：CLIHub 生成的 CLI 应对参数进行转义，或使用 `subprocess` 而非 `os.system`

2. **敏感信息泄露**
   - `--help` 输出可能包含默认值或内部路径信息
   - 令牌文件权限应设置为 `600`（仅所有者可读写）

3. **审计日志**
   - MCP 方案天然有服务器端日志
   - CLI 方案需在 Agent 层记录所有执行的命令（用于调试和审计）

## 对你的意义

如果你正在维护或开发基于 MCP 的 Agent 系统，这个方案值得认真考虑：

**立即试用的理由：**
- 成本节省是实打实的 94%，不是「可能」「大约」
- 实现门槛不高，CLIHub 已开源且支持一键转换
- 不绑定特定平台，未来切换模型提供商无迁移成本

**观望的理由：**
- 如果你的 MCP 工具数量很少（< 10 个），收益有限
- 如果工具 schema 频繁变动，需要评估 CLI 重新生成的频率
- 如果你的 Agent 框架深度集成 MCP 协议，改造可能涉及较多代码

**建议行动：**
1. 统计当前项目的 MCP 工具数量和调用频率
2. 用 CLIHub 为一个 MCP 服务器生成 CLI，做 A/B 测试
3. 对比 token 使用量和 Agent 表现（是否有工具调用失败率上升）

## 关键代码/配置片段

### CLI 风格的 available_tools 配置

```xml
<available_tools>
  <tool>
    <name>notion</name>
    <description>CLI for Notion API</description>
    <location>~/bin/notion</location>
  </tool>
  <tool>
    <name>linear</name>
    <description>CLI for Linear issue tracking</description>
    <location>~/bin/linear</location>
  </tool>
  <tool>
    <name>github</name>
    <description>CLI for GitHub operations</description>
    <location>~/bin/github</location>
  </tool>
  <!-- 6 个工具总计约 300 tokens -->
</available_tools>
```

### 工具调用示例

```bash
# 首次调用：先获取帮助信息（~600 tokens）
$ notion --help
notion search <query> [--filter-property PROPERTY] [--filter-value VALUE]
  Search for pages and databases
notion create-page <title> [--parent-id ID] [--content CONTENT]
  Create a new page in the specified database
notion fetch-page <page-id>
  Fetch full page content
... 12 more tools

# 实际执行（~6 tokens）
$ notion search "Q1 roadmap" --filter-property object --filter-value page
```

### MCP 默认方案对比（供参考）

```json
// MCP 启动时加载的完整 schema（单个工具约 185 tokens）
{
  "name": "notion-search",
  "description": "Search for pages and databases",
  "inputSchema": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "The search query text"
      },
      "filter": {
        "type": "object",
        "properties": {
          "property": { "type": "string", "enum": ["object"] },
          "value": { "type": "string", "enum": ["page", "database"] }
        }
      }
    }
  }
}
// ... 重复 84 次
```

---

## 参考资料

- 原文：[I Made MCP 94% Cheaper (And It Only Took One Command)](https://kanyilmaz.me/2026/02/23/cli-vs-mcp.html) - Kan Yilmaz
- CLIHub: https://github.com/thellimist/clihub
- Anthropic Tool Search: https://www.anthropic.com/engineering/advanced-tool-use
- Openclaw available_skills 格式参考：https://github.com/openclaw/openclaw/blob/9363c320d8ffe29290906752fab92621da02c3f7/docs/concepts/system-prompt.md

---

[← Back to Deep Dives](./README.md)
